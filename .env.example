# Environment Variables Template untuk Docker deployment
# Copy this file to .env.local and fill in your actual values

# Next.js Configuration
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# NextAuth Configuration
NEXTAUTH_URL=https://appiks.vercel.app
NEXTAUTH_SECRET=8LbOphWey/ALOrpJttMNOKTWNHzjtMiMLPJllfmh93wa

# API Configuration
API_BASE_URL=https://appiks-be.disyfa.cloud/api
NEXT_PUBLIC_API_BASE_URL=https://appiks-be.disyfa.cloud/api

# JWT Secret (should match your backend secret for token verification)
JWT_SECRET=your-jwt-secret-should-match-backend

# Database URL (if needed for future features)
# DATABASE_URL=postgresql://username:password@localhost:5432/appiks

# Docker-specific configurations
# Port configuration (default: 3000)
PORT=3000
HOSTNAME=0.0.0.0

# For development with Docker
# Use this for local development with docker-compose
# NEXTAUTH_URL=http://localhost:3000
